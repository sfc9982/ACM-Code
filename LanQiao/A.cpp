//
// Created by sfc9982 on 2022/04/09.
//

#include <algorithm>
#include <cmath>
#include <cstring>
#include <functional>
#include <iomanip>
#include <iostream>
#include <queue>
#include <stack>
#include <string>

using namespace std;

int main()
{
    ios::sync_with_stdio(false);
    cin.tie(nullptr), cout.tie(nullptr);

    int n, m, q;
    cin >> n >> m >> q;
    while (m--)
    {
        int l, r, s;
        cin >> l >> r >> s;
    }
    while (q--)
    {
        int l, r;
        cin >> l >> r;
        cout << "UNKNOWN" << endl;
    }
    return 0;
}